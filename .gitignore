# Originals never go in git.
# Reyna's full-resolution take lives at D:\Reyna-originals\ (~4.4 GB, 416 files).
# Only web-optimised derivatives belong in this repo, under src/assets/.
/originals/
*.CR2
*.CR3
*.NEF
*.ARW
*.dng

# Contact sheets / working files
/contact-sheets/
/scratch/

# Node
node_modules/
dist/
.astro/
.cache/
*.log
npm-debug.log*

# Env
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db
desktop.ini

# Impeccable review captures (large PNGs, regenerate with scripts/shots.mjs)
.impeccable/review/
